name: Build and Release

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
    branches: [main]

env:
  DOTNET_VERSION: '10.0.x'
  PROJECT_PATH: src/Xbox360MemoryCarver/Xbox360MemoryCarver.csproj

jobs:
  build-windows-gui:
    name: Windows GUI (x64)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Restore dependencies
        run: dotnet restore ${{ env.PROJECT_PATH }}

      - name: Build Windows GUI
        run: dotnet publish ${{ env.PROJECT_PATH }} -c Release -f net10.0-windows10.0.19041.0 -r win-x64 --self-contained -o publish/win-gui

      - name: Upload Windows GUI artifact
        uses: actions/upload-artifact@v4
        with:
          name: Xbox360MemoryCarver-Windows-GUI-x64
          path: publish/win-gui/
          retention-days: 30

  build-windows-cli:
    name: Windows CLI (x64)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Restore dependencies
        run: dotnet restore ${{ env.PROJECT_PATH }}

      - name: Build Windows CLI
        run: dotnet publish ${{ env.PROJECT_PATH }} -c Release -f net10.0 -r win-x64 --self-contained -o publish/win-cli

      - name: Upload Windows CLI artifact
        uses: actions/upload-artifact@v4
        with:
          name: Xbox360MemoryCarver-Windows-CLI-x64
          path: publish/win-cli/
          retention-days: 30

  build-linux-cli:
    name: Linux CLI (x64)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Restore dependencies
        run: dotnet restore ${{ env.PROJECT_PATH }}

      - name: Build Linux CLI
        run: dotnet publish ${{ env.PROJECT_PATH }} -c Release -f net10.0 -r linux-x64 --self-contained -o publish/linux-cli

      - name: Upload Linux CLI artifact
        uses: actions/upload-artifact@v4
        with:
          name: Xbox360MemoryCarver-Linux-CLI-x64
          path: publish/linux-cli/
          retention-days: 30

  release:
    name: Create Release
    needs: [build-windows-gui, build-windows-cli, build-linux-cli]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create release archives
        run: |
          cd artifacts
          zip -r ../Xbox360MemoryCarver-Windows-GUI-x64.zip Xbox360MemoryCarver-Windows-GUI-x64/
          zip -r ../Xbox360MemoryCarver-Windows-CLI-x64.zip Xbox360MemoryCarver-Windows-CLI-x64/
          tar -czvf ../Xbox360MemoryCarver-Linux-CLI-x64.tar.gz Xbox360MemoryCarver-Linux-CLI-x64/

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            Xbox360MemoryCarver-Windows-GUI-x64.zip
            Xbox360MemoryCarver-Windows-CLI-x64.zip
            Xbox360MemoryCarver-Linux-CLI-x64.tar.gz
          generate_release_notes: true
